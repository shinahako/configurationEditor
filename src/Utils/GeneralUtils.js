class GeneralUtils {

  static checkIfMapIsNotEmpty(arr) {
    try{
      debugger;
    return Object.keys(arr).length>0;}
    catch(err){
      return false;
    }
  }
  
}

export default GeneralUtils